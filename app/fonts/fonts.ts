import { Caveat, Satisfy,Exo_2,Edu_TAS_Beginner } from "next/font/google";

export const caveat = Caveat({
    subsets: ['latin'],
    display: 'swap',
  })

  export const exo_2 = Exo_2({
    subsets: ['latin'],
    display: 'swap',
  })

  export const edu_tas = Edu_TAS_Beginner({
    subsets: ['latin'],
    display: 'swap',
    weight:"400"
  })

  export const satisfya = Satisfy({
    subsets: ['latin'],
    display: 'swap',
    weight:"400"
  })

  